#!/bin/bash
node_modules/.bin/browserify --debug \
    -r underscore \
    -r react/addons \
    -r superagent \
    -r node-polyglot \
    -r moment \
    -r classnames \
    -r react-router \
    -r react-redux \
    -r redux \
    -r immutable \
    -r reselect \
    > client/public/scripts/bundle-vendors.js
